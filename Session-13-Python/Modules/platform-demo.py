import platform as pt # use pt as alias name

print(pt.release()) # OS release version
print(pt.system()) # OS name
print(pt.version()) # detailed OS version
print(pt.machine()) # Machine Type
print(pt.node()) # Computers Network name hostname
print(pt.processor()) # Processor name
print(pt.python_version()) # Platform Python Versions
print(pt.python_build()) # Platform Python Versions
print(pt.python_compiler()) # Platform Python Versions